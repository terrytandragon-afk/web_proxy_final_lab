# Web 代理服务器课程项目展示总览

项目题目：**设计和实现一个 Web 代理服务器**

本项目使用 Python、Socket、线程和原生 Web 技术实现了一套可运行、可管理、可审计、可重复验收的 Web 代理服务器。系统能够转发客户端的 HTTP 请求，将目标服务器响应返回给客户端，并按照运行中的规则拦截指定域名、URL、请求方法和客户端地址，过滤包含指定关键词的 HTTP 网页正文。

## 1. 题目要求完成情况

| 课程要求 | 项目实现 | 验收方式 |
|---|---|---|
| 转发客户端 Web 请求并返回网页 | 使用 TCP Socket 接收代理格式请求，解析目标地址，连接上游服务器并回传响应 | `python tests\stage4_smoke.py` |
| 拦截指定域名 | 支持完整域名、父域名后缀和显式通配符规则，命中后返回 `403` 页面 | `python tests\stage5_smoke.py` |
| 过滤指定关键词网页 | 检查明文 HTTP 文本响应正文，命中关键词后用 `403` 过滤提示页替代原网页 | `python tests\stage6_smoke.py` |
| 开发语言不限 | 后端使用 Python，管理前端使用 HTML、CSS 和 JavaScript | 查看 `src` 与 `frontend` |

## 2. 系统组成

```text
浏览器 / curl
      |
      | HTTP 代理请求或 HTTPS CONNECT
      v
代理端口 127.0.0.1:8080
      |
      | 客户端访问控制 -> 认证 -> 限流 -> 目标规则 -> 缓存 -> 转发/过滤
      v
目标 Web 服务器

管理前端与 JSON API 127.0.0.1:8088
      |
      | 规则热更新、运行设置、统计、日志查询、CSV 导出
      v
JSON 配置文件 + 文本日志 + JSONL 规则变更记录
```

后端代理端口与管理端口相互独立。客户端 IP 规则只限制代理使用者，管理端口仍能用于修改和恢复规则。

## 3. 已实现功能

### 3.1 核心代理与过滤

- HTTP 请求解析、上游连接、请求改写和响应回传。
- 域名黑名单与域名白名单模式。
- 域名完整匹配、父域名后缀匹配和 `*.example.com`、`*word*` 显式通配符。
- URL 关键词过滤，检查明文 HTTP 请求中的域名、路径和查询参数。
- HTTP 请求方法过滤，可禁止 `PUT`、`DELETE` 等方法。
- HTTP 明文网页正文关键词过滤。
- 浏览器可见的 `403` 拦截提示页，显示原因、命中规则、目标地址与客户端地址。
- HTTPS CONNECT TCP 隧道和 HTTPS 目标域名拦截。

### 3.2 访问控制与运行策略

- 客户端 IPv4、IPv6 和 CIDR 黑名单。
- 客户端 IPv4、IPv6 和 CIDR 白名单，黑名单优先。
- HTTP Basic 代理认证，认证信息不会转发给目标网站。
- 基于客户端 IP 的固定时间窗口访问频率限制。
- HTTP GET 内存缓存，支持 TTL、最大条目数、命中与未命中统计。
- 黑名单模式、白名单模式、缓存、认证和限流参数运行时热更新。

### 3.3 规则管理

- 规则组新增、删除、修改、查询和整组替换。
- 支持管理前端、JSON API 和 `tools/rule_cli.py` 三种管理方式。
- 修改立即作用于新请求，并持久化写回当前 JSON 配置文件。
- 域名、方法和 IP/CIDR 规则输入校验与标准化。
- 规则变更记录持久化为 JSONL，重启后仍可回显。

### 3.4 管理前端与审计

- 首页展示总请求、总拦截、放行、域名拦截、客户端拦截、正文过滤、HTTPS 隧道、缓存、认证和限流统计。
- 规则编辑、运行设置编辑和规则变更回显。
- 访问日志、拦截日志、错误日志与规则变更日志。
- 日志按类型、事件和关键词查询。
- 点击首页统计项进入对应日志详情。
- 日志查询结果导出 CSV。
- 最近一次自动验收证据可在普通管理前端中查看。

## 4. 核心实现技术

### Socket 与线程

代理服务通过 `socket.socket()` 监听 `8080` 端口。每次接受客户端连接后创建独立线程处理，使多个客户端能够并发访问。普通 HTTP 请求由代理重新连接目标服务器；HTTPS 请求使用 `CONNECT` 建立双向 TCP 隧道。

### 规则判定链

`handle_client()` 是每个代理请求的处理入口。处理顺序为：

```text
解析请求
-> 客户端 IP 黑/白名单
-> 代理认证
-> 访问频率限制
-> 方法、域名和 URL 规则
-> GET 缓存
-> HTTP 转发或 CONNECT 隧道
-> HTTP 正文过滤
-> 统计与日志
```

提前拒绝的请求不会连接目标服务器。命中正文规则时，代理已收到目标响应，但不会将原正文交给客户端，而会生成过滤提示页。

### 配置热更新与持久化

运行配置由 `RuntimeState` 统一管理，并使用线程锁保护共享数据。前端或命令行修改规则后，后端对输入进行校验和标准化，更新内存配置，随后写回 JSON 配置文件。新的请求会读取最新配置，因此常用规则和运行设置无需重启代理即可生效。

### 日志与证据

代理将事件写入访问日志、拦截日志和错误日志，规则操作写入 JSONL 变更记录。日志查询 API 将文本记录解析为结构化字段，供管理前端筛选、统计和 CSV 导出。自动验收使用独立证据目录，避免覆盖当前运行日志。

## 5. 重要边界

当前项目属于普通正向代理。对于明文 HTTP，代理能够看到请求路径和响应正文，因此可以执行 URL 与正文关键词过滤。对于 HTTPS，代理通过 CONNECT 转发加密字节流，只能看到目标域名和端口，无法直接读取加密后的路径与正文。

因此：

- HTTPS 域名规则能够生效。
- HTTPS URL 路径和正文关键词过滤需要额外实现中间人解密与证书信任，本项目未启用该能力。
- 正文过滤仅检查文本类型且未压缩的 HTTP 响应，二进制文件和压缩响应会直接放行。

## 6. 快速运行与展示

终端 1，启动确定性的本地测试目标：

```powershell
python tests\manual_demo_server.py
```

终端 2，启动代理和管理前端：

```powershell
python src\proxy.py --config config.example.json
```

管理前端：

```text
http://127.0.0.1:8088/
```

使用独立 Edge/Chrome 配置进行浏览器拦截演示：

```powershell
powershell -ExecutionPolicy Bypass -File tools\start_proxy_browser.ps1
```

一键完整验收：

```powershell
python tests\run_all_smoke.py
```

逐模块现场命令、参数含义和预期输出见：

```text
docs/08_backend_cli_rule_management.md
```

## 7. 测试覆盖

完整测试分为两个互不覆盖证据的批次：

| 批次 | 覆盖内容 | 当前数量 |
|---|---|---:|
| Web/代理功能 | 转发、域名、URL、正文、方法、CONNECT、缓存、前端、认证、限流、浏览器代理与公开 HTTP 示例 | 15 |
| 规则与运行模式 | 管理 API、白名单模式、规则增删改查、CLI、运行设置、客户端 IP/CIDR、规则证据 | 7 |

客户端 IP/CIDR 模块可以单独实测：

```powershell
python tests\stage18_client_ip_policy_smoke.py
```

该测试会实际执行一次正常访问、客户端黑名单拒绝、黑名单修改后恢复、客户端白名单拒绝、白名单修改后放行，并检查统计与日志。

## 8. 主要文件入口

| 文件 | 作用 |
|---|---|
| `src/proxy.py` | 代理协议处理、运行状态、策略链、管理 API 与服务器入口 |
| `src/webproxy/config_rules.py` | 配置读取、规则定义、规则校验和标准化 |
| `src/webproxy/audit.py` | 日志写入、查询、清空和 CSV 导出 |
| `src/webproxy/evidence.py` | 自动验收证据读取与首页统计重建 |
| `frontend/index.html` | 管理首页、规则编辑、运行设置与统计 |
| `frontend/logs.html` | 结构化日志查询与导出 |
| `frontend/changes.html` | 规则变更记录查询 |
| `tools/rule_cli.py` | 后端命令行管理工具 |
| `docs/08_backend_cli_rule_management.md` | 现场逐模块验收总表 |
| `report/final_report.tex` | 期末实验报告 LaTeX 源文件 |

## 9. 项目结论

项目完成了题目要求的 Web 请求转发、指定域名拦截和网页关键词过滤，并在此基础上形成了具有前后端管理、动态规则、访问控制、缓存、认证、限流、日志审计和自动验收能力的完整课程实验系统。所有核心功能都有命令行验收步骤、前端展示入口和自动测试脚本，便于现场说明设计思路与实现过程。
